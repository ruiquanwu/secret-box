<h1>Upload Mutiple Photos</h1>
<p>you can upload mutiple photos to the libarary first, and then drag them to album</p>


<%= form_for [@album, @freephoto] do |form| %>
  <div class="form-group">
    <%= form.file_field :picture, multiple: true %>
  </div>

  <div class="form-group">
    <%= form.submit "Create", class: 'btn btn-success' %>
  </div>
<% end %>